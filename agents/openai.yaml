interface:
  display_name: "Model Fusion"
  short_description: "Fuse the agents available in this environment"
  default_prompt: "Use $model-fusion to gather opinions from the agents available in this environment, then make the final evidence-based decision."
